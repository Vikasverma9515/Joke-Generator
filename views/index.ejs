<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Randome Joke Generator</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h1>Random Joke Generator</h1>
    <form method="get" action="/">
        <label for="category">Choose a category : </label>
        <select name="category" id="category">
            <option value="Any" <%= category === "Any" ? "selected" : ""%>>Any</option>
            <option value="Programming" <%= category === "Programming" ? "selected" : "" %>>Programming</option>
            <option value="Misc" <%= category === "Misc" ? "selected" : "" %>>Misc</option>
            <option value="Dark" <%= category === "Dark" ? "selected" : "" %>>Dark</option>
            <option value="Pun" <%= category === "Pun" ? "selected" : "" %>>Pun</option>
        </select>

        <label for="amount">Number of Jokes :</label>
        <input type="number" id="amount" name="amount" value="<%= amount %>" min="1" max="10" required>
        <button type="submit">Get Jokes</button>
    </form>
    <!-- Display jokes -->
     <div>
        <% jokes.forEach(joke =>{ %>
            <p><%= joke.joke %></p>
       <% }); %> 
     </div>
</body>
</html>